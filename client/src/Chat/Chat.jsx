import React from 'react';

import Header from '../Layout/Header';

const Chat = () => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
};

export default Chat;
